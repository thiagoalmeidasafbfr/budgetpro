version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: budgetpro
      POSTGRES_PASSWORD: budgetpro
      POSTGRES_DB: budgetpro
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U budgetpro"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
